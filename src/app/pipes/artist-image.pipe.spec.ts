import { ArtistImagePipe } from './artist-image.pipe';

describe('ArtistImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistImagePipe();
    expect(pipe).toBeTruthy();
  });
});
